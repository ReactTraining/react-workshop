// import resolveConfig from 'tailwindcss/resolveConfig'
// import tailwindConfig from '../../DELETEtailwind.config.js'

export function getTailwindTheme() {
  // const theme = resolveConfig(tailwindConfig).theme
  return {}
}
