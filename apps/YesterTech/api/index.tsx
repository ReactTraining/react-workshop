import * as auth from "YesterTech/api/auth";
import * as products from "YesterTech/api/products";
import * as users from "YesterTech/api/users";

const api = {
  auth,
  products,
  users,
};

export default api;
