# Build

CRA - Create React App (old)

- Webpack (Build) + Babel (Compiler)

Vite (by the Vue people)

- Rollup (Build) + Babel (Compiler)

# SPA

`npm install react` and a bunch of other stuff

- Router (React Router)
- Global State Management
- Vite (underlying)

# Framework

- NextJS (SSR)
- React Router Framework (Vite)
- Start (uses Vite)

# Unit testing

- Jest (test running, assertion lib, ...)
  - JSDOM (tricks node into thinking it's a browser)
  - HappyDOM
- Vitest
- React Testing Library
