import { Form, Link, type ActionFunctionArgs, type LoaderFunctionArgs } from 'react-router'

export default function Page() {
  return <div></div>
}

/*

app.get('/users', () => {}).   // loader
app.post('/users', () => {}).  // action

*/
